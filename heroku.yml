build:
  docker:
    web: Dockerfile

run:
  web: uvicorn app.api:app --app-dir app --reload --host 0.0.0.0 --port $PORT
  redis:
    image: redis
    command:
      - redis-server --bind 0.0.0.0 --port 6379
